<%= form_with(model: cloth_type) do |form| %>
  <% if cloth_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cloth_type.errors.count, "error") %> prohibited this cloth_type from being saved:</h2>

      <ul>
        <% cloth_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
