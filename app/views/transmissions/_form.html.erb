<%= form_with(model: transmission) do |form| %>
  <% if transmission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transmission.errors.count, "error") %> prohibited this transmission from being saved:</h2>

      <ul>
        <% transmission.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :program_number %>
    <%= form.number_field :program_number %>
  </div>

  <div class="field">
    <%= form.label :person_id %>
    <%= form.text_field :person_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
